package Paquetes;

import java.awt.event.*;
import java.util.LinkedList;

public class ActPoderes {
	public static void  main(String args[]){
/*	
	static LinkedList Poderes = new LinkedList(); //Pila configurada para ser Pila que recibe los poderes al momento de que la moto pase sobre ellos
	boolean inmune = false; //variable que deteminará si el poder Escudo está activo
	boolean veloz = false; //variable para cuando el poder Velocidad está activo	

	public static void main(String[] args) {
		// TODO Auto-generated method stub
	Poderes.add(Escudo.cogePoder());	
	
	KeyEvent e = KeyEvent.getKeyCode();
	
	if(e.getKeyCode() == KeyEvent.VK_E){
		//if (Poderes.getLast() == 1){} 
	
	}
	/*
	public void keyPressed(KeyEvent e){
		   if(e.getKeyCode() == KeyEvent.VK_E){
			   
			   }
	} */
	  /*
		public void keyReleased(KeyEvent e){
			if(e.getKeyCode() == KeyEvent.VK_D){
				derecha = false;
			}
			if(e.getKeyCode() == KeyEvent.VK_A){
				izquierda = false;
			}
			if(e.getKeyCode() == KeyEvent.VK_W){
				arriba = false;
			}
			if(e.getKeyCode() == KeyEvent.VK_S){
				abajo = false;
			} 
		} * 
		/*
	if(e.getKeyCode() == KeyEvent.VK_D){
	//		if (PilaPoderes.pop() == 1){
		}

	
	
			//int cont = Crono.temporizador();
			//while (cont < 5);{
				//inmune = true;
			//}
		//}
	} */
}}
